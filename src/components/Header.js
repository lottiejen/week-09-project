// Create a component <Header> that displays: <h1>Hello, world</h1>

const Header = () => ( // fuction - Header
    <header >
    <h1> Hello, World! </h1> 
    </header>
);

export default Header; // This lets the App.js know that we have created this component and we can refer to it within it.